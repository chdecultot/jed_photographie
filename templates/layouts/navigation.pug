nav.navbar.navbar-default.navbar-fixed-top(role="navigation")
 div.container
  div.navbar-header.page-scroll
    a.navbar-toggle(data-toggle="collapse", data-target="#navbar-collapse")
     i.glyphicon.glyphicon-menu-hamburger
    a.navbar-brand(href='/')
      img(src="images/logo-50x50.png", alt="logo")
  
  div.collapse.navbar-collapse#navbar-collapse
   ul.nav.navbar-nav.navbar-right(id="navbar")
   
    each link in navLinks
              li: a.page-scroll(href=link.href)= link.label
    //if user
    //          if user.canAccessKeystone
    //              li: a(href='/keystone') Open Keystone
    //          li: a(href='/keystone/signout') Sign Out
    //else 
    //          li: a(href='/keystone/signin') Sign In
